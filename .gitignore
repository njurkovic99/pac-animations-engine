# Installed dependencies — the regression harness's devDependencies (playwright,
# pngjs, pixelmatch) are restored with `npm install`, never committed.
node_modules/

# Scratch space for local render/screenshot scripts, and the harness's throwaway
# temp renders (tools/screenshots.mjs writes here, then diffs against tools/baseline).
scratchpad/
tools/.screenshots-tmp/
